version: "2"

linters:
  default: standard
  enable:
    - revive
    - misspell
    - gocritic

  settings:
    revive:
      rules:
        - name: exported
          disabled: true

formatters:
  enable:
    - gofumpt
    - goimports

  settings:
    gofumpt:
      extra-rules: true
