pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.go"
      run: golangci-lint run --fix
      stage_fixed: true
    - name: format
      glob: "*.go"
      run: gofumpt -w {staged_files}
      stage_fixed: true
